let listaUsuarios = new Array();
let listaClientes = new Array();

listaClientes.forEach(element => {
    console.log(element.mostrarInfo());
});

listaUsuarios.forEach(element => {
    console.log(element.mostrarInfo());
});